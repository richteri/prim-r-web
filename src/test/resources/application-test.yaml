application:

  jobQueueKey: jobQueue
  processingQueueKey: processingQueue
  cacheName: primeNumberCache

spring:

  datasource:
    url: jdbc:h2:mem:dev-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  redis:
    host: localhost
    port: 16379

